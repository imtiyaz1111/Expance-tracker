import { baseURL } from "../../Helper/Helper";

const SETTING_ENDPOINTS = {
  CREATE_SETTING: `${baseURL}/user/update-settings`,
  Get_SETTING: `${baseURL}/user/get-settings`,
};

export default SETTING_ENDPOINTS;
